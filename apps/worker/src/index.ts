/**
 * Long-running worker entry (optional).
 * Prefer running individual jobs via npm scripts (`job:ingest-offers`, etc.) from cron.
 */
console.log("Dealernet worker: use package.json scripts for scheduled jobs.");
