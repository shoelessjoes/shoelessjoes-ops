# Local dev (use with: npm run db:migrate)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dealernet_ops?schema=public"

# Railway public proxy — copy fresh URL from dashboard; append sslmode:
# DATABASE_URL="postgresql://USER:PASS@HOST.proxy.rlwy.net:PORT/railway?sslmode=require"
